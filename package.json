{
  "name": "cypress-esbuild-preprocessor",
  "version": "1.0.1",
  "description": "use esbuild as a preprocessor for cypress",
  "main": "dist/index.js",
  "types": "dist/index.d.ts",
  "files": [
    "dist"
  ],
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "build": "tsc",
    "prepublish": "npm run build"
  },
  "keywords": [
    "cypress",
    "preprocessor",
    "esbuild"
  ],
  "repository": {
    "type": "git",
    "url": "https://github.com/sod/cypress-esbuild-preprocessor"
  },
  "author": "alexvonweiss@googlemail.com",
  "license": "ISC",
  "devDependencies": {
    "@types/node": "^14.14.2",
    "esbuild": "^0.7.19",
    "prettier": "^2.1.2",
    "typescript": "^4.0.3"
  }
}
